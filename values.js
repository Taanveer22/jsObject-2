const computer = {
  motherboard: "msi",
  processor: "amd",
  ram: 16,
  ssd: 120,
  case: "gigabyte",
  powerSupply: "corsair",
};

console.log(computer);

const allValues = Object.values(computer);
console.log(allValues);